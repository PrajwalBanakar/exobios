[pytest]
; Local config so pytest doesn't walk up and pick up ../pytest.ini (the app/
; project's config, which points pythonpath at app/ — that would shadow
; ingestion's own `config`/`loaders`/`chunkers` packages with app/'s
; same-named ones when running these tests from ingestion/).
pythonpath = .
testpaths = tests
