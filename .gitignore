# ── Monorepo root .gitignore ──────────────────────────────────────────────────
# Each sub-project has its own .gitignore for project-specific rules.
# This file covers root-level artifacts only.

# Root-level build/install artifacts (anchored to repo root — does not affect sub-projects)
/node_modules/
/dist/
/src/
/.vite/

# Claude Code workspace files
.claude/

# IDE / editor
.vscode/
*.code-workspace
.idea/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Environment files (never commit real secrets)
.env
.env.local
.env.*.local
